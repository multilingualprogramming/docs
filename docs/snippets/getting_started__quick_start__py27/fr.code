import asyncio

async fonction fetch_data(url):
    await asyncio.sleep(0.1)  # simulation d'E/S
    retourner f"Data from {url}"

async fonction main():
    soit result = await fetch_data("https://example.com")
    afficher(result)

asyncio.run(main())
