#!/usr/bin/env python3
import sys
import platform
from multilingualprogramming.runtime.backend_selector import BackendSelector

afficher(f"Python: {sys.version}")
afficher(f"Platform: {platform.system()} {platform.machine()}")

selector = BackendSelector()
afficher(f"WASM Available: {selector.is_wasm_available()}")
afficher(f"Backend: {selector.backend}")
